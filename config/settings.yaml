recording:
  chunk_duration_seconds: 600   # 10 minutes
  sample_rate: 44100            # Default high-fidelity audio
  channels: 1                   # Mono recording
  local_storage_dir: "data_buffer" # Temporary buffer folder

drive:
  parent_folder_id: "Your_Google_Drive_Folder_ID_Here" # Replace with actual Google Drive Folder ID
  target_folder_name: "Bioacoustic_Field_Data"         # Subfolder created dynamically for uploads
